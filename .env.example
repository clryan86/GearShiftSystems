# Use a long random value outside local development.
SECRET_KEY=replace-with-a-strong-secret

# Optional. If omitted, the app uses ./app.db.
# DATABASE_URL=sqlite:///app.db

# PayPal sandbox/client configuration.
PAYPAL_ENV=sandbox
PAYPAL_CLIENT_ID=sb
